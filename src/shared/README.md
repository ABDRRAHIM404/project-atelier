# Shared Source Boundary

Shared code contains stable, provider-neutral concepts used by multiple modules. It cannot import business modules, App Router adapters, infrastructure, database rows, or provider SDKs.
